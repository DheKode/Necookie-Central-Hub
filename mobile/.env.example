# Optional: only needed if you don't want to rely on the repo-root `.env`
# fallback that mobile/app.config.js reads. Copy this file to `.env` in
# this directory and fill in real values. Never commit `.env`.

EXPO_PUBLIC_SUPABASE_URL=your_supabase_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
